{% import 'OroUIBundle::macros.html.twig' as UI %}
{% import 'OroMailChimpBundle::macros.html.twig' as ui %}
{% import 'OroDataGridBundle::macros.html.twig' as dataGrid %}

{% if stats is not null %}
    <strong>{{ 'oro.abandonedcart.conversion.title.summary'|trans }}</strong>
    {{ UI.renderProperty('oro.abandonedcart.conversion.title.orders'|trans, stats.qty) }}
    {{ UI.renderProperty('oro.abandonedcart.conversion.title.total'|trans, stats.total|oro_format_currency) }}

    {{ dataGrid.renderGrid('oro-abandonedcart-campaigns-grid', {'marketingListId': entity.id}) }}
{% endif %}
